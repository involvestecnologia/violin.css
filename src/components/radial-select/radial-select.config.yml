title: Radial Select
status: "ready"
collated: true
variants:
  - name: default
    context:
      radial-select:
        options:
          - "male"
          - "female"
  - name: disabled
    context:
      disabled: true
      radial-select:
        options:
          - "disabled"

