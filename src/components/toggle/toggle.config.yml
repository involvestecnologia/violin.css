title: Toggle
status: "wip"
collated: true
preview: "@preview"
context:
  toggle:
    checked: ''
    disabled: ''
variants:
  - name: default
  - name: checked
    context:
      toggle:
        checked: checked
  - name: disabled
    context:
      toggle:
        disabled: disabled
  - name: checked-disabled
    context:
      toggle:
        checked: checked
        disabled: disabled
  - name: default-accent
    context:
      toggle:
        modifier: inv-toggle__slider--accent
